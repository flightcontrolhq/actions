name: Setup OpenTofu
description: Install a specific version of OpenTofu

inputs:
  opentofu-version:
    description: OpenTofu version to install
    required: false
    default: "1.11.5"

dependencyTemplates:
  - name: tofu
    package: tofu
    installMethod: binary-download
    checkBinary: tofu
    binaryDownload:
      url: https://github.com/opentofu/opentofu/releases/download/v${{ inputs.opentofu-version }}/tofu_${{ inputs.opentofu-version }}_linux_$ARCH.tar.gz
      archiveFormat: tar.gz
      installPath: /usr/local/bin
      architectureSpecific:
        amd64: amd64
        arm64: arm64

instructions: []
